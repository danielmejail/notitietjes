\begin{pregunta}\label{preg:triangulos}
	?`Hay tri\'angulos rect\'angulos is\'osceles de lados enteros?
\end{pregunta}

Si $a$, $b$ y $c$ son las longitudes de los lados, entonces
$c^2=b^2+a^2$ y $b=a$. En particular,
\begin{displaymath}
	c^2\,=\,2a^2
	\dispcomma
\end{displaymath}
%
que no tiene soluci\'on, si $a,c\in\Enteros$ ?`Podemos ``aproximarlo''?
?`Qu\'e es lo m\'as chico que $c^=2a^2$ puede ser?
?`Cu\'al es el valor m\'as chico que puede tomar?
Por ejemplo, podr\'{\i}amos empezar preguntand si
\begin{equation}
	\label{eq:triangulos:dos}
	c^2-2a^2\,=\,\pm 1
\end{equation}
%
tiene soluciones, con $a,c\in\Enteros$, para $+1$ o $-1$.
La respuesta es que s\'{\i}. La \tablename~\ref{tab:triangulos:dos}
muestra algunas de estas soluciones.
\begin{table}
	\centering
	\begin{tabular}{cc|ccc}
		$n$ & $a_n$ & $a$ & $c$ & $c^2-2a^2$ \\
		\hline
		$0$ & $1$ & $1$ & $1$ & $-1$ \\
		$1$ & $2$ & $2$ & $3$ & $1$ \\
		$2$ & $2$ & $5$ & $7$ & $-1$ \\
		$3$ & $2$ & $12$ & $17$ & $1$ \\
		$4$ & $2$ & $29$ & $41$ & $-1$
	\end{tabular}
	%
	\caption{
		Algunas soluciones a $c^2-2a^2=\pm 1$
	}\label{tab:triangulos:dos}
\end{table}
%
Si $(a,c)$ es una soluci\'on a la \eqname~\ref{eq:triangulos:dos},
entonces el conciente $c/a$ aproxima $\sqrt 2$; si $a$ es grande,
$(c/a)^2-2=\pm 1/a^2$ es chica.
En definitiva, dada una soluci\'on $(a,c)$, obtenemos un tri\'angulo
is\'osceles $(a,a,c)$ que es una aproximaci\'on de un tri\'angulo
rect\'angulo de hipotenusa $c$.
\begin{displaymath}
	\begin{tikzpicture}
		\draw
		(0,0) -- ++(1/2,0) -- ++(60-180:1);
		\draw
		(0,0) -- ++(-1/2,0) -- ++(-60:1);
		\draw
		(0,0) -- ++(3/2,0) -- ++(360*13/113-180:2);
		\draw
		(0,0) -- ++(-3/2,0) -- ++(-360*13/113:2);
		\draw
		(0,0) -- ++(7/2,0) -- ++(360*10/79-180:5);
		\draw
		(0,0) -- ++(-7/2,0) -- ++(-360*10/79:5);
	\end{tikzpicture}
	%
\end{displaymath}
%

\begin{pregunta}\label{preg:triangulos:bis}
	Hallar tri\'angulos rect\'angulos de lados enteros
	que difieran lo menos posible.
\end{pregunta}

Si un tri\'angulo as\'{\i} es $(\bar a,\bar b,\bar c)$,
$\bar a$ y $\bar b$ los catetos, entonces podemos
intentar $\bar b=\bar a+1$. De $\bar a^2+\bar b^2=\bar c^2$,
se deduce que
\begin{equation}
	\label{eq:triangulos:bis}
	(2\bar a+1)^2-2\bar c^2\,=\,-1
	\dispstop
\end{equation}
%
Si tenemos una soluci\'on $(a,c)$ de \eqref{eq:triangulos:dos}
con $-1$ del lado derecho y con $c$ impar, definimos
$\bar a\in\Enteros$ tal que $2\bar a+1=c$ ($\bar a=\frac{c-1} 2$)
y $\bar c=a\in\Enteros$. As\'{\i}, obtenemos un tri\'angulo
rect\'angulo $(\bar a,\bar a+1,\bar c)$ (!`primitiva!).
O sea que hay una correspondencia entre tri\'angulos rect\'angulos
$(\bar a,\bar a+1,\bar c)$ y soluciones $(a,c)$ a la ecuaci\'on
con $-1$ y con $c$ impar.
\begin{table}
	\centering
	\begin{tabular}{cc|ccc}
		$a$ & $c$ & $\bar a$ & $\bar b$ & $\bar c$ \\
		\hline
		$1$ & $1$ & $0$ & $1$ & $1$ \\
		$2$ & $3$ & - & - & - \\
		$5$ & $7$ & $3$ & $4$ & $5$ \\
		$12$ & $17$ & - & - & - \\
		$29$ & $41$ & $20$ & $21$ & $29$ \\
		$70$ & $99$ & - & - & - \\
		$169$ & $239$ & $119$ & $120$ & $169$
	\end{tabular}
	%
	\caption{
		Algunas soluciones a \eqref{eq:triangulos:dos}
		y las ternas pitag\'oricas correspondientes
	}\label{tab:triangulos:bis}
\end{table}



